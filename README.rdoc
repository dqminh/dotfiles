My dotfiles

== Installation

  git clone git@github.com:dqminh/dotfiles.git ~/.dotfiles
  cd ~/.dotfiles
  rake install

== Environment

You need zsh, iterm2, prezto, tmux, reattach-to-user-namespace, macvim
